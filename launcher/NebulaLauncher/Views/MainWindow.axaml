<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:NebulaLauncher.ViewModels"
        xmlns:views="using:NebulaLauncher.Views"
        x:Class="NebulaLauncher.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="NebulaGE"
        Width="1060" Height="700"
        MinWidth="820" MinHeight="560"
        TransparencyLevelHint="AcrylicBlur,Blur,Transparent"
        Background="Transparent">

    <!--
        Layers (bottom → top):
          0  Solid NebulaBg floor
          1  Global ambient blob canvas
          2  Sidebar column: its OWN blob layer + acrylic + nav
          3  Content column: transparent — global blobs glow through gaps
    -->
    <Panel>

        <!-- ── 0: Solid background floor ─────────────────────── -->
        <Border Background="{DynamicResource NebulaBgBrush}"/>

        <!-- ── 1: Global ambient blobs ───────────────────────── -->
        <Grid IsHitTestVisible="False">
            <Canvas>
                <!-- Primary — lower-left -->
                <Ellipse Canvas.Left="-160" Canvas.Top="320"
                         Width="680" Height="680"
                         Fill="{DynamicResource NebulaAccentBrush}"
                         Opacity="0.10">
                    <Ellipse.Effect><BlurEffect Radius="100"/></Ellipse.Effect>
                </Ellipse>
                <!-- Secondary — upper-right -->
                <Ellipse Canvas.Left="800" Canvas.Top="-160"
                         Width="460" Height="460"
                         Fill="{DynamicResource NebulaAccentBrush}"
                         Opacity="0.06">
                    <Ellipse.Effect><BlurEffect Radius="80"/></Ellipse.Effect>
                </Ellipse>
                <!-- Accent spark — mid-right -->
                <Ellipse Canvas.Left="860" Canvas.Top="380"
                         Width="220" Height="220"
                         Fill="{DynamicResource NebulaAccentBrush}"
                         Opacity="0.05">
                    <Ellipse.Effect><BlurEffect Radius="50"/></Ellipse.Effect>
                </Ellipse>
            </Canvas>
        </Grid>

        <!-- ── 2+3: Main UI ──────────────────────────────────── -->
        <Grid ColumnDefinitions="200,*">

            <!-- ── Sidebar ──────────────────────────────────────
                 Three layers inside the sidebar Panel:
                   a) ExperimentalAcrylicBorder (blurs what's behind the whole window)
                   b) Sidebar-own blob canvas (visible even without compositor blur)
                   c) Nav content + right separator border
            ─────────────────────────────────────────────────────── -->
            <Panel Grid.Column="0">

                <!-- a) Acrylic blur layer -->
                <ExperimentalAcrylicBorder>
                    <ExperimentalAcrylicBorder.Material>
                        <ExperimentalAcrylicMaterial BackgroundSource="Digger"
                                                      TintColor="{DynamicResource NebulaSurface}"
                                                      TintOpacity="0.80"
                                                      MaterialOpacity="0.92"/>
                    </ExperimentalAcrylicBorder.Material>
                    <Panel/>
                </ExperimentalAcrylicBorder>

                <!-- b) Sidebar-own ambient glow (guaranteed visible) -->
                <Grid IsHitTestVisible="False">
                    <Canvas>
                        <!-- Top-left brand glow -->
                        <Ellipse Canvas.Left="-60" Canvas.Top="-80"
                                 Width="280" Height="280"
                                 Fill="{DynamicResource NebulaAccentBrush}"
                                 Opacity="0.18">
                            <Ellipse.Effect><BlurEffect Radius="65"/></Ellipse.Effect>
                        </Ellipse>
                        <!-- Bottom-left footer glow -->
                        <Ellipse Canvas.Left="-40" Canvas.Top="520"
                                 Width="200" Height="200"
                                 Fill="{DynamicResource NebulaAccentBrush}"
                                 Opacity="0.10">
                            <Ellipse.Effect><BlurEffect Radius="50"/></Ellipse.Effect>
                        </Ellipse>
                    </Canvas>
                </Grid>

                <!-- c) Nav content -->
                <Border BorderBrush="{DynamicResource NebulaBorderBrush}"
                        BorderThickness="0,0,1,0">
                    <DockPanel>

                        <!-- Brand -->
                        <Border DockPanel.Dock="Top"
                                Padding="18,20,16,16"
                                BorderBrush="{DynamicResource NebulaBorderBrush}"
                                BorderThickness="0,0,0,1">
                            <StackPanel Spacing="2">
                                <TextBlock Text="NebulaGE"
                                           FontSize="17"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="v0.1.0"
                                           FontSize="11"
                                           Foreground="{DynamicResource NebulaTextSubtleBrush}"/>
                            </StackPanel>
                        </Border>

                        <!-- Settings pinned to bottom -->
                        <Border DockPanel.Dock="Bottom"
                                Padding="0,6,0,6"
                                BorderBrush="{DynamicResource NebulaBorderBrush}"
                                BorderThickness="0,1,0,0">
                            <Button Classes="ghost"
                                    Classes.active="{Binding IsSettingsSelected}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Padding="16,8"
                                    CornerRadius="0"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="{x:Static vm:NavItem.Settings}">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <Border Width="7" Height="7"
                                            CornerRadius="1"
                                            Classes="nav-icon-box"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text="Settings"
                                               Classes="nav-label"
                                               FontSize="13"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Border>

                        <!-- Nav items -->
                        <StackPanel DockPanel.Dock="Top"
                                    Spacing="1"
                                    Margin="0,12,0,0">

                            <Button Classes="ghost"
                                    Classes.active="{Binding IsProjectsSelected}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Padding="16,9"
                                    CornerRadius="0"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="{x:Static vm:NavItem.Projects}">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <Border Width="7" Height="7"
                                            CornerRadius="1"
                                            Classes="nav-icon-box"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text="Projects"
                                               Classes="nav-label"
                                               FontSize="13"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button Classes="ghost"
                                    Classes.active="{Binding IsEnginesSelected}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Padding="16,9"
                                    CornerRadius="0"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="{x:Static vm:NavItem.Engines}">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <Border Width="7" Height="7"
                                            CornerRadius="7"
                                            Classes="nav-icon-box"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text="Engine Versions"
                                               Classes="nav-label"
                                               FontSize="13"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                        </StackPanel>

                    </DockPanel>
                </Border>

            </Panel>

            <!-- ── Content area ───────────────────────────────── -->
            <ContentControl Grid.Column="1"
                            Content="{Binding CurrentPage}">
                <ContentControl.DataTemplates>

                    <DataTemplate DataType="vm:ProjectHubViewModel">
                        <views:ProjectHubView/>
                    </DataTemplate>

                    <DataTemplate DataType="vm:SettingsViewModel">
                        <views:SettingsView/>
                    </DataTemplate>

                    <DataTemplate DataType="vm:StubPageViewModel">
                        <Grid>
                            <StackPanel HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Spacing="8">
                                <TextBlock Text="{Binding Title}"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Message}"
                                           Foreground="{DynamicResource NebulaTextMutedBrush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>

                </ContentControl.DataTemplates>
            </ContentControl>

        </Grid>

    </Panel>

</Window>
